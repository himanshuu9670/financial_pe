[pytest]
testpaths = tests
pythonpath = .
markers =
    integration: API integration smoke tests (may need DB)
    slow: slow or stress tests
    security: security-focused tests
filterwarnings =
    ignore::DeprecationWarning
